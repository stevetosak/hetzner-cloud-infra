- sudo apt update && apt upgrade
- adduser tosak
- usermod -aG sudo <username>

//.. copy ssh key to <username>@ip ~/.ssh/authorized keys
on pc -> ssh-copy-id -i ~/.ssh/hetzner-cluster.pub <hostname>@<server_ip> // ova so output variables ke go napram za server ipto/hostname

// vpn config for each server
- sudo apt install wireguard -y
- wg genkey | sudo tee /etc/wireguard/private.key | wg pubkey | sudo tee /etc/wireguard/public.key
# ^ sekoj server trebit da gi zacuvam public i private keys vo output variables za da gi koristam posle vo wg0 config

// automated
 - ssh root@<server_ip> /bin/bash << EOF
             apt install wireguard -y
             wg genkey | sudo tee /etc/wireguard/private.key | wg pubkey | sudo tee /etc/wireguard/public.key
             EOF


sudo nano /etc/wireguard/wg0.conf // se popolnuva config za sekoj server
- sudo sysctl -w net.ipv4.ip_forward=1 // ova na cp (control plane) za da forward packets do workerite., plus trebit vo file systcl conf za da e presistent
- sudo systemctl enable wg-quick@wg0
- sudo systemctl start wg-quick@wg0.service





