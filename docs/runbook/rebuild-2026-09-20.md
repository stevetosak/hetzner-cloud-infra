# Cluster rebuild — 2026-09-20

Recovery from total loss of every server. Decisions behind this plan are in
`docs/adr/0001`–`0007`; vocabulary is in `CONTEXT.md`.

**Surviving Hetzner resources** (do not recreate — import them):

| Resource | ID | Name at the loss | Note |
|---|---|---|---|
| network `tosak-net` | 11736362 | `authos-net` | 10.0.0.0/16, 4 subnets |
| firewall `tosak-cp-firewall` | 10289761 | `authos-cluster-firewall` | now the control plane's alone; `tosak-worker-firewall` is new, see ADR 0006 |
| ssh key `tosak-cluster` | 104278281 | `authos-cluster` | |
| primary IP `tosak-cp-ip` | 110296483 | `cp-authos-ip` | 46.62.209.249, unassigned |
| load balancer `tosak-lb` | 6579148 | `authos-lb` | to be deleted, see phase 4 |
| snapshot `k8s-cp-1768482441` | 349712331 | — | old CP disk, 2026-01-15 |

The shared infrastructure was renamed `authos-*` to `tosak-*` during this
rebuild (ADR 0007): `tosak` is the organisation, while `authos` is one of four
applications the cluster hosts. Hetzner names are mutable labels, so every ID
above is unchanged and nothing was recreated. Match by **ID** when reading
anything written before 2026-09-20.

---

## Phase 0 — Safety. Before anything else.

- [x] **Rotate the Hetzner API token.** Done, per the operator. Hetzner exposes
      no token listing, so this cannot be confirmed over the API — the live
      token works, which is not the same as proving it is the new one.
      Confirm the history line was scrubbed with other zsh sessions closed.
- [x] Set delete protection on snapshot `349712331`. **Verified over the API:**
      `protection.delete = true`. Remove the protection and delete the snapshot
      only once this rebuild is verified (Phase 6).
- [x] Create the Cloudflare R2 bucket and an S3-compatible access key. Bucket
      `hetzner-cloud-infra`; endpoint account `5621ce9e64f6881e8e6229573adf3b36`.
      Proven working — `shared/` state is in it.
- [x] Generate an age keypair. Done, per the operator; the private key is in
      the password manager, which is where it belongs — **not** the repo, not
      the cluster. Losing it loses every secret. Nothing to verify from here,
      and deliberately so: a key this session could check would be a key in the
      wrong place.

      Phase 5 needs the **public** key, which is not secret, committed as the
      age recipient in a top-level `.sops.yaml`. Have it to hand then.
- [x] Copy `infra/terraform.tfstate*` somewhere offline before restructuring.
      The files are also still on disk, now untracked and gitignored.

## Phase 1 — Terraform restructure. No applies yet.

- [x] Split `infra/` into `shared/`, `control-plane/`, `workers/` (ADR 0002).
      Cross-module references are hcloud data sources **by name**.
- [x] `shared/` manages the network, the four subnets and the firewall — and
      also the ssh key and the primary IP, which were previously `data`
      sources. ADR 0002 assigns both to `shared/`; left unmanaged, the repo
      could not rebuild into an empty Hetzner project.
- [x] `control-plane/` manages only `k8s-cp`, with `prevent_destroy = true`,
      `delete_protection = true`, `rebuild_protection = true`.
- [x] `workers/` manages only the worker map. `var.node_suffix` deleted with no
      replacement — worker names are stable: `k8swk1`, `k8swk2`, `k8swk3`
      (ADR 0005). No `create_before_destroy`: Hetzner names must be unique
      among live servers.
- [x] Configure the `s3` backend against R2, `use_lockfile = true`, one key per
      module — written in each `backend.tf`, and initialised. R2 accepted
      `region = "auto"`.
- [x] **Import** the surviving network, subnets, firewall, ssh key and primary
      IP into `shared/`. They exist; creating them would be wrong. Ran
      `infra/shared/import.sh` — state-only, idempotent. All eight addresses
      are in `tfstate/shared.tfstate` on R2. State locking via `use_lockfile`
      is confirmed working against R2.
- [x] `git rm --cached` the tfstate files and the 39 MB of vendored provider
      binaries under `infra/.terraform/`; both added to `.gitignore`.
      Per-module `.terraform.lock.hcl` files stay tracked.
- [x] Verify the import. The check is **zero create, zero destroy, zero
      replace** — not literally "no changes".

      Result: **`Plan: 0 to add, 2 to change, 0 to destroy.`** Gate passed.
      Both changes are the expected ones, and **neither has been applied**:

      1. `hcloud_primary_ip.cp_authos_ip` — `delete_protection` false → true.
      2. `hcloud_firewall.authos_cluster_firewall` — the port 22 rule is
         removed, because `allow_public_ssh` defaults to false. The WireGuard
         rule renders as a remove-and-add pair; that is how Terraform diffs a
         set of blocks when one member leaves, and its contents are unchanged.

      Phase 2 needs SSH, so it sets `allow_public_ssh = true`, which puts the
      port 22 rule back pinned to the workstation's current address. Details in
      `infra/README.md`.
- [x] `control-plane/` and `workers/` backends initialise against R2. Neither
      holds state yet; they own nothing until Phases 2 and 3.

- [x] Apply the two changes. Authorised by the operator after the gate passed,
      so `shared/` now matches its configuration. `Apply complete! Resources:
      0 added, 2 changed, 0 destroyed.`

      **Verified directly against the Hetzner API, not from the apply output:**
      the firewall now carries the WireGuard rule alone, with `0.0.0.0/0` on
      UDP 51820 intact — confirming the remove-and-add pair in the diff was
      only Terraform redrawing the set. Primary IP `46.62.209.249` reports
      `protection.delete = true` and `auto_delete = false`.

      `terraform plan` then returned **`No changes.`** with
      `-detailed-exitcode` 0.

**Phase 1 is complete and converged.**

## Phase 2 — Control plane. Write the runbook as you go.

Every command in this phase goes into `docs/runbook/control-plane.md` verbatim
as it is run. That file is the deliverable, not a by-product (ADR 0004).

- [x] Reopen SSH and land the ADR 0006/0007 changes. `shared/` applied with
      `allow_public_ssh = true` from a **saved plan**, so what ran is what was
      reviewed: `Apply complete! Resources: 1 added, 4 changed, 0 destroyed.`

      **Verified over the Hetzner API, not from the apply output:** network
      11736362 is `tosak-net` with all four subnets and zero servers; firewall
      **10289761** is `tosak-cp-firewall` — the same ID, so it was renamed and
      not recreated — carrying UDP 51820 from `0.0.0.0/0` and TCP 22 from
      `185.100.244.43/32`; the new firewall **11651947** is
      `tosak-worker-firewall`, carrying TCP 22 and nothing else; ssh key
      104278281 is `tosak-cluster`; primary IP 110296483 is `tosak-cp-ip`,
      `46.62.209.249`, still delete-protected and unassigned.

      A follow-up `terraform plan` returned **`No changes.`** with
      `-detailed-exitcode` 0.

      ⚠️ Port 22 is OPEN to this workstation until the last bullet of this
      phase closes it (ADR 0004).

- [x] `terraform apply` in `control-plane/` — creates `k8s-cp` on 10.0.1.5 and
      attaches primary IP 46.62.209.249, so the SSH and VPN endpoint is
      unchanged.
- [x] Base host setup: user, sysctl, containerd.
- [x] WireGuard hub. A new keypair is generated, so **the laptop peer config
      must be updated** with the new server public key.
- [x] Write the Control Plane Endpoint into `/etc/hosts` **before** `kubeadm
      init`, or init cannot resolve its own endpoint:
      `10.0.1.5  k8s-cp.tosak.internal`
- [x] `kubeadm init` with:
      `--control-plane-endpoint=k8s-cp.tosak.internal:6443`   ← see ADR 0006
      `--apiserver-advertise-address=10.0.1.5`
      `--pod-network-cidr=10.244.0.0/16`
      `--service-cidr=10.96.0.0/16`   ← narrowed, see ADR 0001
      `--apiserver-cert-extra-sans=10.100.0.1,46.62.209.249`

      kubeadm adds the endpoint name to the certificate itself. The two extra
      SANs are the operator route over the VPN, and the public address kept
      only as break-glass — port 6443 is closed to the world and stays closed.
- [x] Set the kubelet to `--cloud-provider=external` **before** init. Without
      it the node gets no `providerID` and the CSI driver cannot attach a
      volume. The worker pipeline does this; nothing did it for the CP.
- [x] Fetch kubeconfig to the laptop, and rewrite `server:` to
      `https://10.100.0.1:6443`. kubeadm writes the Endpoint name, which the
      laptop deliberately does not resolve — the operator route is the VPN
      address, and it is in the certificate. Merge as a NEW context; do not
      overwrite `kubernetes-admin@kubernetes` until the new one answers.
- [x] Flannel, pod CIDR 10.244.0.0/16, MTU 1400.
- [x] hcloud cloud-controller-manager, with the network ID set so nodes leave
      the `uninitialized` taint. Without it nothing will ever schedule.
- [x] hcloud-csi-driver, and set `hcloud-volumes` as the **default**
      StorageClass (ADR 0005). No host prerequisites needed — unlike Longhorn.
- [x] Verify: control plane node `Ready`, no lingering cloud taint.

- [x] **Close bootstrap SSH.** `allow_public_ssh` was one variable gating port
      22 on BOTH firewalls, so closing it would also have closed the Worker
      firewall that Phase 3 needs. Split into `allow_public_ssh_cp` and
      `allow_public_ssh_worker` first, both defaulting false, then applied from
      a saved plan: `Plan: 0 to add, 2 to change, 0 to destroy.` →
      `Apply complete! Resources: 0 added, 2 changed, 0 destroyed.`

      🔴 **The apply lied about half of it.** hcloud provider 1.69.0 cannot take
      a firewall from one rule to none: it reported success, wrote the empty
      rule set to state, and left TCP 22 standing on `tosak-worker-firewall`.
      The API accepts an empty rule set — `set_rules {"rules":[]}` returned 201
      — so the provider is the limit. Cleared by that call; `terraform plan`
      then returned `No changes.` with `-detailed-exitcode` 0. Details and the
      verification commands are in `infra/README.md` and step 9 of
      `docs/runbook/control-plane.md`.

      **Verified over the Hetzner API:** `tosak-cp-firewall` 10289761 carries
      `in udp 51820 0.0.0.0/0` and nothing else; `tosak-worker-firewall`
      11651947 carries no rules at all. From the workstation,
      `nc -vz 46.62.209.249 22` times out while `ssh cp-dev@10.100.0.1` and
      `kubectl get --raw=/readyz` both still work.

**Phase 2 is complete.** `docs/runbook/control-plane.md` is its deliverable.

## Phase 3 — Workers

**Phase 3 is COMPLETE.** `docs/runbook/workers.md` is its deliverable.

- [x] `terraform apply` in `workers/` — three servers named `k8swk1..3`.
      `Plan: 3 to add, 0 to change, 0 to destroy.` The names carry no suffix of
      any kind, so the mechanism that destroyed the cluster on 2026-09-13
      cannot be expressed by the module.
- [x] **Built by hand, runbook-first — `kluster` was NOT used and NOT fixed.**
      This reverses the original plan for this phase, deliberately. Neither
      `kluster` nor `infra/scripts/bootstrap/bootstrap_workers.sh` was usable:
      five defects were known or found before a command was run, three of them
      new (see the head of `docs/runbook/workers.md`). Fixing either first
      would have made the first-ever Worker build also the first run of
      unproven code, which is the opposite of what ADR 0004 asks for. Porting
      the proven procedure into `kluster` moves to **Phase 6**, where the
      plan-gate work already lives.
- [x] All three joined over the **VPN**, not over the bootstrap port, proving
      the operator path before that port was withdrawn.
- [x] Verify: four nodes `Ready`, `providerID` on every node, Flannel bound to
      `enp7s0` everywhere, pod MTU 1400 end to end, cross-node pod-to-pod ICMP
      and 4 MiB TCP both clean, CoreDNS resolving to `10.96.0.1`. The
      `hcloud-csi-controller` pod that was Pending through all of Phase 2
      scheduled itself onto `k8swk1` seconds after the first join.
- [x] **Closed `allow_public_ssh_worker` and verified the close over the API.**
      🔴 **The provider defect reproduced exactly as predicted.** Terraform
      completed without error and the rule was still standing at Hetzner: port
      22 open on all three Workers while state said closed. Cleared with
      `set_rules {"rules":[]}`, which also queues one `apply_firewall` per
      attached server. Re-verified: worker firewall carries no rules, `nc -vz`
      to port 22 on all four public addresses is filtered, both modules plan
      `No changes.` with exit code 0.

### Carried into Phase 6, not done here

- [ ] Port `docs/runbook/workers.md` into `kluster`: drop the suffix
      machinery, drop `LonghornStage` and `LonghornPreflight`, take one path
      per Terraform root module, and carry the five corrections the runbook
      records. Full plan-gate work is Phase 6 as before.

## Phase 4 — Cluster services

**The ingress decision is settled. See ADR 0008.** ingress-nginx is not
reinstalled; the cluster moves to Gateway API with **Envoy Gateway**, keeping
Flannel. Read the ADR before starting — several things this checklist used to
say were wrong, and the reasons matter.

Order is not free here. Gateway API CRDs must exist before Envoy Gateway, and
before cert-manager, which needs an explicit `--enable-gateway-api` and refuses
to start the feature without the CRDs.

- [x] **Gateway API standard-channel CRDs.** Standard channel only. Nothing in
      the design needs `TLSRoute` or the experimental channel; if ArgoCD's gRPC
      forces a fallback to passthrough, that is when the experimental CRDs get
      reconsidered, and it is a change to this ADR.
- [x] **Envoy Gateway.** Pin the version in the manifest, as `core/cni` and
      `core/hcloud` do.
- [x] **cert-manager**, with `--enable-gateway-api`
      (`config.enableGatewayAPI=true`), plus a **DNS-01 `ClusterIssuer` using
      Cloudflare**. The token is scoped `Zone:DNS:Edit` on `tosak.net` alone.
      One wildcard `Certificate` for `*.tosak.net`, issued into the `gateway`
      namespace. It does not cover the apex; no host uses the apex.
      **This issues before the load balancer exists** — that is the point of
      choosing DNS-01, so do it in this order and not after.
- [x] **The `gateway` namespace and one shared `Gateway`.** One HTTPS listener
      with the wildcard certificate, one HTTP listener. `allowedRoutes` names
      the namespaces permitted to attach: `argocd`, `authos`, `doma`.
- [x] **`EnvoyProxy` carrying the Hetzner annotations.** Under
      `provider.kubernetes.envoyService.annotations`, taken from the deleted
      `core/load-balancer/load-balancer.yaml`: `name: tosak-lb`, `type: lb11`,
      `location: hel1`, `use-private-ip: "true"`,
      `private-subnet-ip-range: 10.0.4.0/24`, `private-ipv4: 10.0.4.2`,
      `uses-proxyprotocol: "true"`, `algorithm-type: round_robin`.
      **Drop the mqtt port 1883** — wasteio is out of scope (ADR 0003).
- [~] **`ClientTrafficPolicy` on the Gateway** — (1) and (2) done, **(3) is
      NOT done**. Carrying three things that must land together:
      1. `enableProxyProtocol: true`. The Hetzner annotation and this setting
         are two halves of one thing. PROXY headers arriving at a listener that
         does not expect them break **every** connection.
      2. `clientIPDetection.customHeader: CF-Connecting-IP`. Every host is
         proxied by Cloudflare, so the PROXY-protocol address is a Cloudflare
         edge address, not the visitor's.
      3. `tls.clientValidation.caCertificateRefs` holding the **per-zone**
         Cloudflare origin-pull certificate, with Authenticated Origin Pulls
         enabled on the zone. Without it the header in (2) is forgeable by
         anyone who finds the origin address. Cloudflare's *shared* origin-pull
         certificate does not do this job — it is the same for every Cloudflare
         customer.
      🔴 **Do not reach for a `SecurityPolicy` IP allowlist instead.**
      `clientCIDRs` matches the *detected* client address, so with (2) set it
      checks the forgeable header against itself. ADR 0008 explains why that is
      worse than no lock at all.
- [x] **Delete load balancer `6579148`**, let the CCM build a fresh one from
      the `EnvoyProxy` annotations, then update the four Cloudflare A records
      to its new address. Low risk: all five hosts resolve to Cloudflare edge
      addresses, so this is an origin change behind the proxy and not a
      propagation event for visitors.
- [x] **Delete the old ingress entirely:** `core/ingress-controller/` (both
      classes — `private-nginx` had no consumer left but the Longhorn UI, which
      ADR 0005 removes), `core/load-balancer/load-balancer.yaml`,
      `core/whoami-test-ingress.yaml`, `core/longhorn/`.
- [x] **Convert the `Ingress` objects to `HTTPRoute`.** `ingress2gateway` does
      the mechanical part; the output needs reading.
      - **Delete every `URLRewrite` filter it emits.** The three
        `rewrite-target: /` annotations were **inert** — ingress-nginx skips the
        rewrite when the path equals the target, and every path in those three
        objects is `/`. Gateway API passes the path through unchanged, which is
        the live behaviour. `/duster` on `authos-demo.tosak.net` must reach
        Duster with its path intact, and that is now the default.
      - `argocd` loses passthrough: set `argocd-server` to `--insecure`,
        terminate at the Gateway, and put `appProtocol: kubernetes.io/h2c` on
        the `argocd-server` Service port so the CLI's gRPC still works.
        **Acceptance test: `argocd login` and `argocd app list` over
        `argocd.tosak.net` must pass.** If they do not, the fallback is
        passthrough, which costs the experimental CRDs — and that is a change to
        ADR 0008, not a quiet fix.
      - Apply routes for `argocd`, `authos`, `authos-api`, `authos-demo` and
        `doma` only. Convert and commit `imaps` and `wasteio`, but do not apply
        them: their manifests stay in the repo unsynced (ADR 0003).
- [ ] ~~Longhorn~~ — removed entirely (ADR 0005). No install, no ingress, no
      `basic-auth` secret, no host prerequisites.
- [x] CNPG operator, `tosak-pg-cluster` on `storageClass: hcloud-volumes`,
      databases `authos` and `doma` only. **First real volume attach in the
      rebuilt cluster**, so it is the first real test of the CSI driver.
      **Done 2026-09-21** — steps 9 and 10 of `docs/runbook/cluster-services.md`.
      CloudNativePG v1.30.0, PostgreSQL **18.6** (the image is now pinned; it
      was not), 3 instances one per worker, ready in 3m04s. The CSI driver
      worked first time: three 10 GB Hetzner volumes in `hel1`, verified
      attached over the provider API. Two gaps this uncovered, both recorded
      there: nothing in this repository created the `db-credentials` Secret,
      and a hand-made one silently breaks password rotation unless it carries
      `cnpg.io/reload=true`.
- [ ] Fix `core/monitoring/lean-monitoring.yaml`: both `nodeSelector` values
      point at the dead `k8swk2-20260105-182701`; set them to `k8swk2`. Move
      Prometheus to `emptyDir`. SOPS the Grafana `adminPassword`, which is
      currently committed as `changeme123`.
- [x] Redis. **Done 2026-09-21** — step 11 of
      `docs/runbook/cluster-services.md`. `redis:8.10.2-alpine` pinned by
      digest, one replica in namespace `redis`, no persistence. The two files
      that existed carried six defects: no namespace, no `credentials.yaml`
      (nothing created the `redis` Secret, the same gap `db-credentials` had),
      a floating minor image tag, `maxmemory` equal to the container limit, a
      `RollingUpdate` strategy on a single stateful replica, and no
      `securityContext` — which mattered because `command:` replaces the image
      entrypoint and so skips the privilege drop, leaving **Redis running as
      root**. Also recorded there: the Secret was first stored with a trailing
      newline and the pod went `Ready` regardless.
- [x] **ArgoCD, 2026-09-21.** Step **12** of `docs/runbook/cluster-services.md`.
      v3.5.3, chart 10.9.2, a pinned helm render in the `core/gateway` shape.
      Nothing in this repository had ever installed ArgoCD, so there was
      nothing to correct but the route — which was wrong. 🔴 ADR 0008's
      `appProtocol: kubernetes.io/h2c` cannot go on one Service port:
      `argocd-server` splits the UI from gRPC with cmux and the UI matcher is
      HTTP/1.x only, so h2c on the only port drops every browser request.
      Two Service ports and two route rules instead. The notification config
      moved out of a tracked ConfigMap into the values file, where it is no
      longer reverted by re-applying the render.
- [ ] One **ApplicationSet** over the project overlays. No hand-made
      Applications this time (ADR 0003).

**Two new credentials this phase creates**, both for the Phase 5 inventory: the
Cloudflare DNS token, and the per-zone origin-pull certificate.

- [x] **`README.md` here and `CLAUDE.md` at the workspace root** both documented
      two ingress-nginx classes. Corrected on 2026-09-21, together with four
      other stale claims they carried — see step 7 of
      `docs/runbook/cluster-services.md`. Note this repository's `CLAUDE.md` is
      **gitignored**, so that correction is local to one workstation.

### Ingress stack status, 2026-09-21

**The Public Entry Point is BUILT and PROVEN.** `docs/runbook/cluster-services.md`
is the record, steps 1 to 7. Envoy Gateway v1.9.1, Gateway API v1.6.2 standard,
cert-manager v1.21.2, wildcard certificate issued, Hetzner load balancer
`7907558` rebuilt on `77.42.14.48` and `10.0.4.2`, PROXY protocol proven end to
end through Cloudflare with the visitor's real address reaching the backend.

**ADR 0008 was amended the same day.** Seven statements of fact in it were
wrong, including two field names, the order of the load-balancer delete, and a
set of doma annotations that are not inert. Read its Amendments section before
acting on any specific claim in its body.

**Still open in this phase:**

- [ ] **The origin lock.** Authenticated Origin Pulls on the Cloudflare zone,
      a per-zone origin-pull certificate, and
      `ClientTrafficPolicy.tls.clientValidation.caCertificateRefs`. Held to
      last on purpose: turning validation on before Cloudflare presents the
      certificate breaks every host at once, with no way in but the VPN. Until
      it is done, `CF-Connecting-IP` is forgeable by anyone who finds
      `77.42.14.48`. **A `Zone:DNS:Edit` token cannot do this** — it needs a
      second token or the dashboard.
- [ ] **Apply the routes.** The ArgoCD route went on with ArgoCD on
      2026-09-21. **Eight are still committed and unapplied** — authos,
      authos-api, authos-demo, doma, imaps, imaps-api, wasteio and
      wasteio-api. Their namespaces do not exist yet, and imaps and wasteio
      are out of scope per ADR 0003. Phase 5.
- [x] **The `argocd` gRPC acceptance test — PROVEN AT THE ORIGIN, 2026-09-21.**
      A plain gRPC call straight at the load balancer returned `grpc-status: 0`
      and the server version, served by `rule/0` over h2c. Terminating at the
      Gateway does carry the CLI's gRPC; no `TLSRoute` is needed.
      🔴 **Cloudflare returns 403 for `application/grpc` on this zone** — the
      block is content-type specific, so it is the zone's gRPC setting and not
      a WAF rule, and a `Zone:DNS:Edit` token cannot change it. This costs
      nothing: the CLI retries over gRPC-web by itself and the second rule
      serves it.
      🔴 **The first version of this test was worthless and looked like a
      pass.** The CLI skips its gRPC probe when the local config says
      `grpc-web: true`, which it did, left over from the ingress-nginx
      cluster. Force a real attempt with `--config <an empty path>`.
- [x] **`argocd login` and `argocd app list` — PASSED, 2026-09-21.** The
      operator logged in; `get-user-info` returns `Logged In: true,
      Username: admin` and `argocd-server` logged
      `grpc.code=OK grpc.service=application.ApplicationService`. Served on
      `rule/1`, because the CLI fell back to gRPC-web at the Cloudflare edge.
      A fresh login writes the config **without** `grpc-web`, so each later
      call loses one round trip at the edge and prints a warning — cosmetic;
      log in with `--grpc-web` or enable the zone setting.
- [ ] Decide what `tosak.net` and `www.tosak.net` should serve. Both are
      proxied A records that now reach the Gateway and match no route, so they
      return 404. The certificate covers both.

## Phase 5 — Secrets and applications

- [ ] Recreate each secret and, in the same step, commit its
      `credentials.enc.yaml` encrypted with age. A secret that is created
      without being encrypted into git has not been restored, only recreated.
      Inventory: `credentials` and `keystore`/`keystore-pass` and `duster-admin`
      (authos); `credentials` (doma); `db-credentials` (pg-cluster); `redis`;
      `argocd-notifications-secret`; the Grafana admin password. The
      `basic-auth` secret is gone with Longhorn. **Two are new in Phase 4**
      (ADR 0008): the Cloudflare DNS-01 API token, and the per-zone
      Cloudflare origin-pull certificate the Gateway validates against.
- [ ] `/etc/keystore/*.p12` is still on the laptop — reuse it.
- [ ] Re-provision the Duster demo `client_id` via `bootstrap.ts`, then update
      `authos-demo-config`.
- [ ] Verify each host serves: authos, authos-api, authos-demo, doma.

## Phase 6 — Make it survivable

- [ ] CNPG barman-cloud to R2: WAL archiving, daily base backup, 30-day
      retention.
- [ ] Restore drill: scheduled restore into a throwaway cluster, asserting row
      counts, failing loudly. Monthly.
- [ ] Port `docs/runbook/control-plane.md` into `kluster cp init` stages. CNI
      is its own `Stage` so Cilium can replace Flannel later (ADR 0001).
- [ ] kluster: plan mode by default, intent assertion, abort under `--apply` on
      mismatch, close `allow_public_ssh_worker` after bootstrap (ADR 0004).
      Ensure
      `node remove` deletes the Kubernetes Node object — names are reused now.
- [ ] Only now: delete snapshot 349712331.
