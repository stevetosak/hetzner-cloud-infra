# Committed as a template with blank values only — it documents what secrets exist.
# Real values are applied via projects/doma/scripts/init_secrets.sh, never committed.
apiVersion: v1
kind: Secret
metadata:
  name: credentials
  namespace: doma
type: Opaque
stringData:
  DATABASE_URL: ''
  SESSION_SECRET: ''
  GOOGLE_CLIENT_SECRET: ''
