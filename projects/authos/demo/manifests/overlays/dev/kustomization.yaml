apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: authos

resources:
  - ../../base

images:
  - name: stevetosak/authos-demo
    newTag: latest

replicas:
  - count: 1
    name: authos-demo
